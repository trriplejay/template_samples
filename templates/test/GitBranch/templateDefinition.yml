resources:
  - name: gittest_{{ .Values.name | default "" | replace "-" "_" }}
    type: GitRepo
    configuration:
      gitProvider: github
      path: trriplejay/simpleserver
      branches:
        include: {{gitBranch}}



